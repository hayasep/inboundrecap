services:
  - type: web
    name: backstock-filler-emailer
    env: python                 # ← change from runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k gthread -t 120 app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /envcheck  # ← optional but handy
    envVars:
      - key: EXCEL_TEMPLATE_PATH
        value: backstock report.xlsx
      - key: FLASK_SECRET
        generateValue: true
      - key: SMTP_SERVER
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USERNAME
        sync: false      # set in the Render dashboard
      - key: SMTP_PASSWORD
        sync: false      # set in the Render dashboard (Gmail App Password)
      - key: SENDER_EMAIL
        sync: false      # set in the Render dashboard
      - key: DEFAULT_RECIPIENT
        value: ""
      - key: UNIFORM_ROW_HEIGHT_PX
        value: "18"
